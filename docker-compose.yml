services:
  frontend:
    build: .
    container_name: confhub-frontend
    restart: unless-stopped
    network_mode: host
    env_file: .env.local
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
